// This function takes an array as input and returns a new Set object
// containing the elements of the array.
function setFromArray(array) {
  return new Set(array);
}

// Exporting the setFromArray function as the default export of this module.
export default setFromArray;
